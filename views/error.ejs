<%
	let style = '';
	let body = '';
	let script = '';
%>

<% style = `` %>

<% body = `<div class="content-wrapper">
			<div class="content-header row">
				<div class="content-header-left col-md-9 col-12 mb-2">
					<div class="row breadcrumbs-top">
						<div class="col-12">
							<h2 class="content-header-title float-left mb-0">Error</h2>
							<div class="breadcrumb-wrapper">
								<ol class="breadcrumb">
									<li class="breadcrumb-item">
										<a href="/">Home</a>
									</li>
									<li class="breadcrumb-item">
										<a href="javascript:void(0);">Error</a>
									</li>
								</ol>
							</div>
						</div>
					</div>
				</div>
			</div>
			<div class="content-body">
				<h1>${message}</h1>
				<h2>${error.status}</h2>
				<pre>${error.stack}</pre>
			</div>
		</div>`%>

<% script = `` %>

<%- include('layout', { title: `${error.status} - Error`, style, body, script }) %>